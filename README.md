My first functional personal website, built with Django.
I recently implemented a live chat feature with persistence using LocalStorage.
The chat is built using Django Channels, Redis, Django REST Framework, and JavaScript.

It's deployed in render, at the free tier, wich caused me alot of stress lol,
Had to use a script to deploy, 'cause render doesn't allow console for the poor.

The design is from figma, you can see more at /projects :)
Feel free to clone and make your own ideas real, just add me on linkedin(optional).
